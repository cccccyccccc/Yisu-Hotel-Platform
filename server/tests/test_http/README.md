## http测试目录

### 1.测试文件书写规范
要求文件名为"test_测试部分.http"。

### 2.测试流程
#### 2.1. **顺序执行：** 先跑 `test_auth` 拿到 Token -> 再跑 `test_hotel` 建房 -> 跑 `test_rooms` 加价格 -> 跑 `test_admin` 审核 -> 最后跑 `test_public` 验证。
#### 2.2. **变量替换：** WebStorm 的 `.http` 文件支持 `@变量名 = 值` 的写法，直接在文件头部改一次，下面所有的请求都会自动用那个值，不用一个个粘贴。