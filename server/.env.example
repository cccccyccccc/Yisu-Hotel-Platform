# =====================================
# 易宿酒店平台 - 环境变量模板
# =====================================
# 复制此文件为 .env 并填写实际值
# cp .env.example .env

# --- 基础配置 ---
PORT=5000
NODE_ENV=production

# --- 数据库 ---
# Docker 环境使用服务名 mongodb
MONGODB_URI=mongodb://mongodb:27017/yisu-hotel

# 本地开发使用 localhost
# MONGODB_URI=mongodb://127.0.0.1:27017/yisu-hotel

# --- Redis ---
# Docker 环境使用服务名 redis
REDIS_URL=redis://redis:6379

# 本地开发使用 localhost
# REDIS_URL=redis://127.0.0.1:6379

# --- JWT 密钥 (必填！) ---
# 请生成一个强密钥，例如: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_here_change_this

# --- 测试数据库 (可选) ---
# MONGODB_URI_TEST=mongodb://127.0.0.1:27017/yisu_test_db

# --- Docker 镜像源 (可选) ---
# 使用免登录镜像源加速拉取
DOCKER_REGISTRY=8hezaxuxay1o4w.xuanyuan.run
